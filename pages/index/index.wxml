<view class="searchInput">
<view data-id="{{id}}" class="searchContent">
{{searchText}}
</view>
</view>

<view class="nav">
<view class="item" wx:for="id" wx:for="{{navs}}" wx:for-item="v" wx:for-index="i">{{v.name}}{{i}}
</view>
</view>
